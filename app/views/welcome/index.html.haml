%section#welcome-bar-section
  %h1
    Want your projects to work regular as clockwork?
  %h1
    -unless user_signed_in?
      = link_to "JOIN US!", new_user_registration_path, :class => 'btn btn-lg btn-primary'
    -else
      You are in the right place!

=gflash