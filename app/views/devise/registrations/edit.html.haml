%h2
  Edit #{resource_name.to_s.humanize}
  
.col-xs-12.col-sm-9.col-md-6   
  = simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
    =f.input :first_name, label: 'First name'
    =f.input :last_name, label: 'Last name'
    =f.input :email, as: :email, label: 'Email'
    - if devise_mapping.confirmable? && resource.pending_reconfirmation?
      %p
        Currently waiting confirmation for: #{resource.unconfirmed_email}
    =f.input :password, as: :password, label: 'Password', hint: "(leave blank if you don't want to change it)"
    =f.input :password_confirmation, as: :password, label: 'Password confirmation'
    =f.input :current_password, label: 'Current password', hint: "(we need your current password to confirm your changes)"
    =f.button :submit
    
%h3 Cancel my account
%p
  Unhappy? #{button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete}
